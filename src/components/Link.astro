---
import { Icon } from "astro-icon/components";
const { data } = Astro.props;
---

<a href={data.href} class="btn btn-outline btn-xl btn-block text-lg">
  <Icon name={data.icon} height="1em" width="1em" />
  {data.name}
</a>
